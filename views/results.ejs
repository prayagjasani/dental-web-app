<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results - Dental Patient Management</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body>
    <div class="container">
        <div class="app-header">
            <span style="font-size: 40px;">🦷</span>
            <h1>Search Results</h1>
        </div>

        <div class="records-section">
            <% if (patients && patients.length > 0) { %>
                <div class="records-list">
                    <% patients.forEach(patient => { %>
                        <div class="record-card">
                            <div class="record-header">
                                <h3>
                                    <%= patient.first_name %>
                                        <%= patient.last_name %>
                                </h3>
                                <div class="dates">
                                    <span class="visit-date">
                                        Last Visit: <%= patient.formatted_last_visit_date || 'Not recorded' %>
                                    </span>
                                </div>
                            </div>
                            <% if (patient.contact_number) { %>
                                <p class="contact">
                                    <strong>Contact:</strong>
                                    <%= patient.contact_number %>
                                </p>
                                <% } %>
                                    <% if (patient.medical_history) { %>
                                        <p class="medical-history">
                                            <strong>Medical History:</strong>
                                            <%= patient.medical_history %>
                                        </p>
                                        <% } %>
                                            <% if (patient.treatment_notes) { %>
                                                <p class="treatment-notes">
                                                    <strong>Treatment Notes:</strong>
                                                    <%= patient.treatment_notes %>
                                                </p>
                                                <% } %>
                                                    <div class="card-actions">
                                                        <form action="/delete/<%= patient.id %>" method="POST">
                                                            <button type="submit" class="delete-btn" onclick="return confirm('Are you sure you want to delete this patient?')">
                                        <i class="fas fa-trash"></i> Delete
                                    </button>
                                                        </form>
                                                    </div>
                        </div>
                        <% }); %>
                </div>
                <% } else { %>
                    <p class="no-records">No patients match your search criteria. Try adjusting your search terms.</p>
                    <% } %>
        </div>

        <div class="links">
            <a href="/search">New Search</a>
            <a href="/">Back to Dashboard</a>
        </div>
    </div>
</body>

</html>